const markdown = require('../../samples').markdown.basic
const code = require('../../samples').code.basic

export const content = [markdown, code];
export const title = 'Basic';
export const link = 'basic';

export const title = 'Form Walk Through';
export const link = 'form-walk-through';
export const markdown = require('../../samples').markdown['form-walk-through'];
export const code = require('../../samples').code['form-walk-through'];
